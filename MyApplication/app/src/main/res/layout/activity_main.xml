<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
        xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:tools="http://schemas.android.com/tools"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        tools:context=".MainActivity"
        android:padding="10dp">


    <Button
            android:text="Show Toast"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:id="@+id/btnToast"
            android:layout_alignParentTop="true" android:layout_marginTop="75dp"
            android:layout_centerHorizontal="true"/>
    <Button
            android:text="Send to next intent"
            android:layout_width="match_parent"
            android:layout_height="wrap_content" android:id="@+id/btnSendToNext"
            android:layout_below="@+id/btnToast"/>
    <EditText
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:hint="Enter your text here"
            android:ems="10"
            android:id="@+id/editText"
            android:layout_below="@+id/btnSendToNext"/>
</RelativeLayout>